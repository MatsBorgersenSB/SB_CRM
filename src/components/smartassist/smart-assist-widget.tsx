/**
 * Floating SmartAssist widget removed (Phase UX).
 * Intelligence is embedded natively in each workspace.
 * Conversational capabilities remain via Universal Search (Ctrl+K) and workspace actions.
 */
export function SmartAssistWidget() {
  return null;
}

export function SmartAssistLauncher() {
  return null;
}
